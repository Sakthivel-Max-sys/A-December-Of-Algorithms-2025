# Taking input as space-separated operations
operations = input().split()

stack = []

for op in operations:
    if op == "C":
        stack.pop()
    elif op == "D":
        stack.append(stack[-1] * 2)
    elif op == "+":
        stack.append(stack[-1] + stack[-2])
    else:
        stack.append(int(op))

print(sum(stack))


sample input 
5 2 C D +

sample output
30

